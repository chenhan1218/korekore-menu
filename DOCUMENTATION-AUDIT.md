# 文檔審計報告 (Documentation Audit Report)

**日期**: 2025-12-23
**狀態**: 進行中
**目標**: 更新文檔以反映實際現代碼實現

---

## 概述

通過對實現代碼的詳細審計，發現文檔（特別是 ARCHITECTURE.md）與實際實現存在顯著差異。本報告列出所有不符之處和更新計劃。

---

## 🔴 主要發現

### 1. 架構模式差異

#### 文檔描述
- **文檔名稱**: 六邊形架構 (Hexagonal Architecture)
- **結構**: 三層模型 (Domain, Infrastructure, UI) + Ports & Adapters 模式
- **特徵**: 業務邏輯完全獨立於 UI 框架，通過 Port 接口與 Infrastructure 通信

#### 實際實現
- **實際架構**: Clean Architecture (3 層)
- **現狀**:
  - 目錄結構已按照 Hexagonal 規劃 (domain/ports, domain/entities, domain/usecases)
  - 但實際代碼使用**簡化版本**:
    - `MenuScanDomain` 類直接調用 `mockMenuScanService`
    - 未實現完整的 Port/Adapter 接口依賴注入
    - 業務邏輯與 Mock 服務有直接耦合
  - Zustand stores 直接在 domain/stores/ 中，不通過 Port 抽象

#### 差異評估
```
文檔承諾度: ████████░ 80% 已實現框架結構
實現完整度: ██████░░░ 60% 實際使用簡化版本
不符程度:   🔴 中等 - 結構對但邏輯簡化
```

---

### 2. 核心模塊實現差異

#### Domain 層

**文檔描述:**
```
src/domain/
├── entities/            # MenuItem, MenuData 等業務實體
├── usecases/            # parseMenu, saveMenu 等業務流程
├── value-objects/       # SelectedItems, OrderPrice 等值對象
├── ports/               # GeminiPort, FirebasePort 接口
└── types/               # Domain 層特有類型
```

**實際實現:**
```
src/domain/
├── entities/            ✅ MenuData.ts, MenuItem.ts (已實現)
├── usecases/            ✅ parseMenuImage.ts (已實現)
├── ports/               ✅ FirebasePort, GeminiPort, LocalStoragePort (已實現)
├── stores/              ✅ menuStore.ts (Zustand state management)
└── menuScan.ts          ✅ 實現 MenuScanDomain 業務邏輯類

❌ 缺失:
- value-objects/ 目錄（未實現）
- 完整的 usecase 實現（只有 parseMenuImage）
- Port 接口的完整應用（存在但未被充分使用）
```

#### Infrastructure 層

**文檔描述:**
```
src/infrastructure/
├── services/            # GeminiService, FirebaseService 實現
├── repositories/        # MenuRepository, ImageRepository
├── adapters/            # Ports 實現 (GeminiAdapter, FirebaseAdapter)
└── config/              # 初始化與配置
```

**實際實現:**
```
src/infrastructure/
├── adapters/            ✅ GeminiAdapter.ts (存在)
├── mockMenuService.ts   ✅ Mock 菜單數據服務

❌ 缺失:
- services/ 目錄（未分離）
- repositories/ 目錄（未實現）
- config/ 目錄（未實現）
- 完整的 Firebase/Gemini services
```

#### UI 層

**文檔描述:**
```
src/ui/
├── react/               # React 實現 (可替換)
│   ├── adapters/        # useParseMenu 等 Hooks
│   ├── components/      # UI 組件
│   ├── stores/          # Zustand 狀態管理
│   └── pages/           # 頁面組件
└── [vue/]/              # Vue 實現（未來可替換）
```

**實際實現:**
```
src/ui/
├── components/          ✅ MenuUploadInput, MenuItemCard, MenuList
├── pages/               ✅ MenuScanPage, HomePage
├── hooks/               ✅ useMenuUploadState.ts
├── react/               ✅ 目錄存在（用於 React 適配）
└── [未實現] adapters/   ❌ React 適配層位於 hooks/，而非 adapters/

❌ 缺失:
- react/adapters/ 目錄結構（Hook 直接在 hooks/）
- 多框架可替換性（無 vue/, svelte/ 目錄）
```

#### Shared 層

**文檔描述:**
```
src/shared/
├── types/               # 全局類型定義
└── utils/               # 通用工具函數 (imageProcessing, errorHandler, i18n)
```

**實際實現:**
```
src/shared/             ✅ 目錄存在
├── types/              ✅ 存在（內容需檢查）
└── [內容]

額外的:
src/types/              ✅ 存在（菜單、API 類型）
- menu.ts
- 重複的類型定義？

❌ 問題:
- src/types/ 與 src/shared/types/ 重複
- 工具函數位置: src/shared/imageCompression.ts 存在
```

---

### 3. 關鍵功能實現狀況

#### 已完成 (Phase 1-4 MVP)

| 功能 | 狀態 | 位置 | 註記 |
|------|------|------|------|
| 菜單掃描基礎設施 | ✅ | src/domain/menuScan.ts | 簡化版本，直接調用 Mock 服務 |
| Mock 菜單數據服務 | ✅ | src/infrastructure/mockMenuService.ts | 20 個示例菜單項 |
| 菜單掃描頁面 | ✅ | src/ui/pages/MenuScanPage.tsx | 完整功能 |
| 菜單項卡片展示 | ✅ | src/ui/components/MenuItemCard.tsx | 含規格選擇 |
| 勾選狀態管理 | ✅ | src/domain/stores/menuStore.ts | Zustand store |
| 圖片上傳與驗證 | ✅ | src/ui/components/MenuUploadInput.tsx | 含壓縮 |
| 集成測試 | ✅ | src/ui/pages/__tests__/MenuScanPage.integration.test.tsx | 14 個測試 |

#### 未完成 (計劃中)

| 功能 | 文檔承諾 | 實際進度 | 影響 |
|------|---------|---------|------|
| Gemini API 整合 | ✅ 已記錄 | 未實現 | 當前用 Mock 服務替代 |
| Firebase 整合 | ✅ 已記錄 | 未實現 | 菜單歷史同步功能 |
| 多語系支援 | ✅ 已記錄 | 部分 | 界面支援中英文 |
| 點餐卡生成 | ✅ 已記錄 | 未實現 | 不在 MVP 範圍內 |

---

## 📋 文檔更新清單

### 優先級 1: 關鍵更新（影響理解架構）

#### [ ] README.md
- **問題**: 最後更新日期為 2025-12-17，已過期
- **更新項目**:
  - [ ] 更新最後修改日期
  - [ ] 更新技術棧說明（當前狀態 vs 未來計劃）
  - [ ] 調整項目結構說明以匹配實際代碼
  - [ ] 澄清 MVP 功能 vs 完整功能
  - [ ] 更新環境變數說明（移除未使用的）

#### [ ] docs/ARCHITECTURE.md
- **問題**: 詳細描述六邊形架構，但實現是簡化版本
- **更新項目**:
  - [ ] 改名為"清潔架構（Clean Architecture）與六邊形架構規劃"
  - [ ] 第 1 章：**當前實現** (MVP 階段)
    - 說明當前為簡化版本 (不用完整 Port/Adapter)
    - 為何如此設計（快速迭代）
  - [ ] 第 2 章：**計劃路線圖** (未來升級)
    - 何時升級到完整六邊形架構
    - 升級路徑
  - [ ] 移除對未實現功能的詳細說明
  - [ ] 重點說明實際實現的代碼結構

#### [ ] docs/DECISIONS.md
- **問題**: 特別是 ADR-011（六邊形架構決策）與實現不符
- **更新項目**:
  - [ ] 更新 ADR-011: 補充"階段式實現"說明
  - [ ] 新增 ADR-012: 「簡化實現以加速 MVP」
  - [ ] 記錄技術債務：未來需升級的項目

---

### 優先級 2: 補充文檔（幫助新開發者）

#### [ ] docs/CURRENT-IMPLEMENTATION.md (新文件)
- 詳細說明當前實現狀態
- 與文檔對比
- 未實現功能列表
- 技術債務清單

#### [ ] docs/ROADMAP.md (新文件)
- 升級路線圖
- 何時實現完整六邊形架構
- 何時集成 Gemini API
- 何時集成 Firebase

#### [ ] docs/API-INTEGRATION.md
- **問題**: 詳細說明 Gemini 與 Firebase，但均未實現
- **更新項目**:
  - [ ] 標記為"計劃文檔"
  - [ ] 移動實現說明到"計劃"部分
  - [ ] 當前階段說明：使用 Mock 服務

---

### 優先級 3: 更新現有文檔

#### [ ] docs/CODE-STANDARDS.md
- [ ] 驗證現實代碼是否遵循標準
- [ ] 更新任何不符之處

#### [ ] docs/FEATURE-CHECKLIST.md
- [ ] 更新完成度反映實際進度
- [ ] 標記 MVP 功能 vs 計劃功能

#### [ ] docs/ARCHITECTURE-EVOLUTION.md
- [ ] 驗證內容適用性
- [ ] 清理對未實現細節的描述

---

## 🎯 更新建議

### 方案 A: 最小更新（保守做法）
```
時間投入: 4-6 小時
範圍:
  - 更新 README.md（最後更新日期、澄清 MVP 狀態）
  - 在 ARCHITECTURE.md 開頭加註"MVP 簡化版本"
  - 新增 CURRENT-IMPLEMENTATION.md
  - 更新 DECISIONS.md ADR-011 註記
好處: 快速，最小化改動
缺點: 仍保留大量"規劃"vs"實現"混淆
```

### 方案 B: 中度更新（推薦）
```
時間投入: 8-12 小時
範圍:
  - 完整重寫 ARCHITECTURE.md（分為當前 + 計劃）
  - 新增 CURRENT-IMPLEMENTATION.md（詳細說明實現）
  - 新增 ROADMAP.md（升級路線圖）
  - 更新 README.md
  - 調整 API-INTEGRATION.md 為計劃文檔
好處: 清晰區分當前 vs 計劃，新開發者易理解
缺點: 需要投入更多時間
```

### 方案 C: 完整更新（激進做法）
```
時間投入: 16-20 小時
範圍:
  - 重構所有文檔
  - 升級代碼以匹配文檔（實現完整六邊形架構）
  - 新增所有缺失的目錄與接口
好處: 代碼與文檔 100% 對齊
缺點: 時間投入大，延遲其他功能開發
```

---

## 📊 詳細差異列表

### 目錄結構對比

```
文檔規劃                          實際實現                         狀態
────────────────────────────────────────────────────────────────
src/domain/entities/             src/domain/entities/             ✅
src/domain/usecases/             src/domain/usecases/             ⚠️ (僅 1 個)
src/domain/value-objects/        ❌ 不存在                         ❌
src/domain/ports/                src/domain/ports/                ✅
src/domain/stores/               ❌ 文檔未提到                     ✅

src/infrastructure/services/     ❌ 不存在(內容在 adapters)        ❌
src/infrastructure/repositories/ ❌ 不存在                         ❌
src/infrastructure/adapters/     src/infrastructure/adapters/     ⚠️ (僅 1 個)
src/infrastructure/config/       ❌ 不存在                         ❌
src/infrastructure/mockMenuService.ts ✅ 實際存在               ✅

src/ui/react/adapters/           ❌ 實際在 src/ui/hooks/           ⚠️
src/ui/react/components/         src/ui/components/               ⚠️
src/ui/react/pages/              src/ui/pages/                    ⚠️
src/ui/react/stores/             src/domain/stores/               ⚠️
src/ui/[vue]/                    ❌ 不存在                         ❌

src/shared/types/                src/shared/types/               ⚠️
src/shared/utils/                src/shared/                      ⚠️

src/types/                        src/types/                       ❌ (與 shared 重複)
```

---

## 🔧 具體改進清單

### 代碼結構 (可同步進行)

- [ ] 整理 types/ 目錄（合併 src/types 與 src/shared/types）
- [ ] 建立 src/ui/react/adapters/ 目錄結構
- [ ] 驗證 Zustand stores 放置位置的合理性

### 文檔結構 (推薦方案 B)

- [ ] README.md: 更新日期 + 澄清 MVP
- [ ] ARCHITECTURE.md: 重新組織為"當前 + 計劃"
- [ ] CURRENT-IMPLEMENTATION.md: 新增（詳細實現說明）
- [ ] ROADMAP.md: 新增（升級路線圖）
- [ ] DECISIONS.md: 更新 ADR-011 + 新增 ADR-012

---

## 🎓 新開發者上下文改進

### 目前問題
- 新開發者看文檔會假設完整的六邊形架構
- 當查看代碼時，會發現許多不符的地方
- 不清楚哪些是"計劃"vs"實現"

### 建議改進
1. **首頁澄清** (README)
   - 明確說明"當前狀態：MVP 簡化版本"
   - 指向 ROADMAP.md 了解未來計劃

2. **實現說明** (CURRENT-IMPLEMENTATION.md)
   - "這是我們目前的做法"（代碼 as truth）
   - 解釋為什麼這樣設計（快速迭代優先）

3. **升級路線** (ROADMAP.md)
   - 何時升級各個組件
   - 升級順序

4. **架構決策** (DECISIONS.md)
   - 記錄為何選擇簡化實現
   - 記錄未來的計劃變更

---

## ✅ 下一步行動

**推薦方案**: 方案 B（中度更新）

1. **此周**: 確認用戶同意採用方案 B
2. **第 1-2 天**: 重寫 ARCHITECTURE.md（當前 + 計劃）
3. **第 2-3 天**: 新增 CURRENT-IMPLEMENTATION.md 與 ROADMAP.md
4. **第 3 天**: 更新 README.md 與其他文檔
5. **最後**: 檢查代碼與文檔對齊

---

**報告完成日期**: 2025-12-23
**下一步決策需求**: 用戶批准更新方案
